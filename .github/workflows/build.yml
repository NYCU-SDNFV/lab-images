name: build and validate candidate images
on:
  push:
    branches: [main]
    paths: ['base/**', 'tests/**', '.github/workflows/build.yml', '.github/workflows/multiarch-test.yml']
  workflow_dispatch:
permissions:
  contents: read
  packages: write
jobs:
  candidate:
    uses: ./.github/workflows/multiarch-test.yml
